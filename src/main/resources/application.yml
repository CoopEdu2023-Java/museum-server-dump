spring:
  datasource:
    url: jdbc:mysql://localhost:3306/moonshot_museum_database
    username: backend_access
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect